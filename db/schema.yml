# Data schema for water tank monitoring system data

# Single database titled 'main'
main:
  # DOCUMENTS
  config: # Software configuration settings
  # Data is logged via records using sequential UUIDs
  # (auto-generated 32 hex char incrementing filenames)